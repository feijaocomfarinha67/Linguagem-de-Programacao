from multas import calcular_infração

limite_via = int(input("Informe limite da via "))
velocidade_carro = int(input("Informe a velocidade do carro "))

calcular_infração(limite_via, velocidade_carro) 